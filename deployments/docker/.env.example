# Artifusion Docker Deployment Configuration
# Copy this file to .env and configure your settings

# ============================================
# GitHub Authentication
# ============================================

# GitHub Personal Access Token for proxying GitHub Packages
# Required scopes: read:packages
# Get token at: https://github.com/settings/tokens
GITHUB_PACKAGES_TOKEN=

# Optional: GitHub username for GitHub Packages Maven repository
# Required only if using GitHub Packages in Reposilite configuration
GITHUB_PACKAGES_USERNAME=

# Optional: Restrict access to specific GitHub organization
# Leave empty to allow any authenticated GitHub user
GITHUB_ORG=

# Optional: Restrict to specific teams within the organization
# Only checked if GITHUB_ORG is set. Comma-separated list.
GITHUB_TEAMS=

# GitHub authentication cache TTL (reduces API calls ~99%)
# Format: 30m, 1h, 24h
AUTH_CACHE_TTL=30m

# ============================================
# Backend Authentication
# ============================================

# Reposilite Admin Token (REQUIRED for backend authentication)
# Generate with: openssl rand -hex 32
# This enables authentication between Artifusion and Reposilite
# If not set, Reposilite backend authentication will be disabled
REPOSILITE_ADMIN_TOKEN=

# NOTE: Docker Registry and Verdaccio run without backend authentication
# They are only accessible through Artifusion on a private Docker network

# ============================================
# Upstream Registry Credentials (Optional)
# ============================================

# Credentials for private upstream registries (e.g., private GHCR, Docker Hub, etc.)
# JSON format: {"registry.example.com": {"username": "user", "password": "token"}}
UPSTREAM_CREDENTIALS=

# ============================================
# Logging Configuration
# ============================================

# Artifusion log level: debug, info, warn, error
LOG_LEVEL=info

# Artifusion log format: json, console
LOG_FORMAT=console

# Verdaccio log level: http, debug, info, warn, error
VERDACCIO_LOG_LEVEL=info
